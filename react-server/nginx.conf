events {}

http {
    server {
        listen 80;
        server_name VigilHawk;

        root /usr/share/nginx/html;
        index index.html;

        location / {
            try_files $uri $uri/ /index.html;
        }
    }
}
